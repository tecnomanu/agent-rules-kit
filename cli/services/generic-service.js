import { BaseService } from './base-service.js';

export class GenericService extends BaseService {
  // Currently, no stack-specific overrides or new methods are needed.
  // Additional generic stack specific logic can be added here in the future.
  constructor(options) {
    super(options);
  }
}
